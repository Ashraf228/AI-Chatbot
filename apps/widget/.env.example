# The widget reads runtime configuration from the loader script and public API.
# No required local env variables are currently needed for the Vite widget build.
# Public URLs are configured in the API/Dashboard env:
# PUBLIC_API_BASE_URL
# PUBLIC_WIDGET_BUNDLE_URL
# NEXT_PUBLIC_WIDGET_LOADER_URL
